<ui:FluentWindow x:Class="Nekomata.UI.FindReplaceWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        mc:Ignorable="d"
        Title="查找和替换" Height="300" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ExtendsContentIntoTitleBar="True"
        WindowBackdropType="Mica"
        WindowCornerPreference="Round">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0"/>

        <Grid Grid.Row="1" Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <TextBlock Text="查找内容(原文/译文):" VerticalAlignment="Center" Margin="0,0,10,10"/>
            <ui:TextBox x:Name="FindTextBox" Grid.Column="1" Margin="0,0,0,10"/>

            <TextBlock Grid.Row="1" Text="替换为:" VerticalAlignment="Center" Margin="0,0,10,10"/>
            <ui:TextBox x:Name="ReplaceTextBox" Grid.Row="1" Grid.Column="1" Margin="0,0,0,10"/>

            <StackPanel Grid.Row="3" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Bottom">
                <ui:Button Content="查找下一个" Click="FindNext_Click" Margin="0,0,10,0"/>
                <ui:Button Content="替换" Click="Replace_Click" Margin="0,0,10,0"/>
                <ui:Button Content="全部替换" Click="ReplaceAll_Click" Appearance="Primary"/>
            </StackPanel>
        </Grid>
    </Grid>
</ui:FluentWindow>