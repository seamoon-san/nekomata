<ui:FluentWindow x:Class="Nekomata.UI.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Nekomata.UI"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        mc:Ignorable="d"
        Title="{DynamicResource Settings_Title}" Height="550" Width="500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ExtendsContentIntoTitleBar="True"
        WindowBackdropType="Mica"
        WindowCornerPreference="Round"
        Icon="/Assets/icon.ico">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ui:TitleBar Title="{DynamicResource Settings_Title}" Icon="pack://application:,,,/Assets/icon.ico" Grid.Row="0"/>

        <TextBlock Grid.Row="1" Text="{DynamicResource Settings_Title}" FontSize="20" FontWeight="Bold" Margin="20,10,20,10" Foreground="{DynamicResource TextFillColorPrimaryBrush}"/>

        <ScrollViewer Grid.Row="2" Margin="20,0,20,20">
            <StackPanel>
                <ui:CardControl Icon="{ui:SymbolIcon LocalLanguage24}" Header="{DynamicResource Settings_Language}" Margin="0,0,0,10">
                    <ComboBox ItemsSource="{Binding AvailableLanguages}" 
                              SelectedItem="{Binding SelectedLanguage}" 
                              DisplayMemberPath="Key"
                              Width="200" HorizontalAlignment="Left" Margin="0,10,0,0"/>
                </ui:CardControl>

                <ui:CardControl Icon="{ui:SymbolIcon DarkTheme24}" Header="{DynamicResource Settings_Theme}" Margin="0,0,0,10">
                    <ComboBox ItemsSource="{Binding AvailableThemes}" 
                              SelectedValue="{Binding SelectedTheme}" 
                              SelectedValuePath="Value"
                              DisplayMemberPath="Key"
                              Width="200" HorizontalAlignment="Left" Margin="0,10,0,0"/>
                </ui:CardControl>
            </StackPanel>
        </ScrollViewer>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="20,0,20,20">
            <ui:Button Content="{DynamicResource Settings_Save}" Command="{Binding SaveSettingsCommand}" CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}" Appearance="Primary" Width="80" Margin="0,0,10,0"/>
            <ui:Button Content="{DynamicResource Settings_Cancel}" IsCancel="True" Width="80"/>
        </StackPanel>
    </Grid>
</ui:FluentWindow>
